#include<stdio.h>
int main()
{
    int b, h, area;
    printf("Input the base and hieght:");
    scanf("%d%d",&b,&h);
    area=0.5*b*h;
    printf("The area of a triangle is:%d",area);
    return 0;
}
   